
## Pre-compaction note (1:17 AM)
Context hit 91%. Archive saved to memory/sessions/2026-03-18.md and posted to Discord #memory.
OR key not available in this env — balance unknown.
Session covered extensive johnhib.tech work — see archive for full detail.

## Session (1:21 AM)

### field-notes back button
- Changed back button on all field note detail pages from "← Field Notes" (/field-notes) to "← johnhib.tech" (https://johnhib.tech)
- File: `src/app/field-notes/[slug]/page.tsx`
- Deployed to production: `johnhib-tech-5zht9njsk` → live at johnhib.tech

### Vercel deploy pattern (confirmed working)
- rootDirectory in Vercel project settings = `projects/johnhib-tech/site` (relative to workspace root)
- Must run Vercel CLI from workspace root `/Users/maria/.openclaw/workspace-main/`
- Copy `.vercel/` from site dir to workspace root first (if not there)
- Then: `vercel build --prod` + `vercel deploy --prebuilt --prod`
- Running from site subdir causes path doubling and fails
