# Session Archive — 2026-03-18 (pre-compaction)

**Context at compaction:** 91% (73k/80k tokens) | Model: anthropic/claude-sonnet-4-6
**OR live data:** unavailable (401 in this environment)

---

## What We Built Tonight (Tue Mar 17 → Wed Mar 18)

### johnhib.tech — Major session

#### OG Images
- Extended `scripts/generate-og.mjs` to also generate cards for field notes
- Field notes use first 140 chars of body as excerpt, tagged "Field Notes"
- Added `openGraph` + `twitter` meta tags to field note slug pages
- First card live: `it-keeps-forgetting-and-i-keep-showing-up.png`

#### Corner Anchors (VersionBadge + ContactButton)
- Font bumped again: `0.72rem` → `0.79rem` (+10%)
- Positioning changed: `clamp(1rem, 2vw, 1.5rem)` → `clamp(1.25rem, 3vw, 2rem)` for better iPad visibility
- Added `display: block`, `transform: translateZ(0)` for GPU compositing
- Added `data-corner-anchor` attribute + CSS transition in globals for smoother resize
- iPad Pro 11" 3rd gen visibility fix applied

#### Cycling Subhead — johnhib.tech homepage
- New `CyclingSubhead.tsx` client component — same crosshair click mechanic as .com
- Three lines (workshopped with John):
  1. "Documenting what works before I forget how I got here."
  2. "The promise is real. So is the gap."
  3. "No manual. Just what breaks and what doesn't."
- Replaced static tagline "Building with AI, documented as it happens."

#### Contact Modal — Progressive Disclosure UX Demo
- Default state: name / subject / message / open-in-mail-app
- On name field focus: email field + "Send now" button ease in simultaneously (downward only)
- Easing: `0.32s cubic-bezier(0.4, 0, 0.2, 1)` on `max-height`, `opacity`, `margin-top`
- Modal anchored `flex-start` with `paddingTop: clamp(4rem, 12vh, 8rem)` — expands downward only
- Fields stay revealed once shown (no collapse)

#### Resend Integration
- Installed `resend` package
- Created `/api/contact/route.ts` — POSTs to Resend, delivers to `hello@johnhib.com`
- `RESEND_API_KEY` added to Vercel env (not in codebase)
- "Send now": loading → "Sending…" → "Sent ✓" (green) → auto-close after 1.8s
- Error state surfaces with fallback to mail app
- "Open in mail app" unchanged, always visible
- Note: currently sends from `onboarding@resend.dev` — needs domain verification to use `hello@johnhib.com`

#### Bottom Fade Gradient
- Fixed white gradient overlay in layout — covers all pages
- Height: 14rem, solid white from 35% to bottom, fade above
- Masks corner anchors + eliminates content overlap
- `pb-32` added to all page `<main>` wrappers (homepage, writings slug, field notes index, field note slug)

#### Field Note — "It Keeps Forgetting and I Keep Showing Up"
- First field note written and live
- Shared with Ankur for feedback
- Suggested message: "Started a field notes section on my site — short observations from actually building with AI. This is the first one. Would love your take. johnhib.tech/field-notes/it-keeps-forgetting-and-i-keep-showing-up"

---

## Key Decisions
- Field notes are separate from articles — standalone observations, ~200 words, no demo required
- Contact modal is intentionally a UX demo — progressive disclosure as proof of concept
- Resend is the send backend; "Open in mail app" stays as permanent fallback
- Bottom fade is a design pattern for the corner anchor zone — applies globally via layout

## Open / Next Steps
- Resend domain verification (send from `hello@johnhib.com` instead of `onboarding@resend.dev`)
- johnhib.com version sync (still pending — both sites should update version tag together)
- Gradient fade position may need further tuning (John was iterating on height/reveal line)
- Write more field notes
- Continue Sphere project (Stage 1 was active before this session)
